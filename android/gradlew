#!/usr/bin/env sh

DIR=`cd "$(dirname "$0")" && pwd`
GRADLE_WRAPPER="$DIR/gradle/wrapper/gradle-wrapper.jar"

if [ ! -f "$GRADLE_WRAPPER" ]; then
  echo "Gradle wrapper not found."
  exit 1
fi

java -Xmx64m -classpath "$GRADLE_WRAPPER" org.gradle.wrapper.GradleWrapperMain "$@"
